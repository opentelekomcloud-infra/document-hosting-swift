apiVersion: v1
kind: Service
metadata:
  name: {{ include "document-hosting-swift.fullname" . }}
  labels:
    {{- include "document-hosting-swift.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "document-hosting-swift.selectorLabels" . | nindent 4 }}
